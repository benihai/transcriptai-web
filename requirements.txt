# --- Core engine ---
faster-whisper==1.1.0          # תמלול מקומי (Whisper) - תומך עברית, רץ על CPU/GPU
google-genai==1.21.0           # חיבור ל-Gemini לסיכום ומשימות
python-dotenv==1.0.1           # טעינת מפתחות מקובץ .env

# --- Recording (שלב 3) ---
sounddevice==0.5.1             # הקלטת מיקרופון
soundcard==0.4.3               # הקלטת קול המערכת (loopback) ב-Windows
numpy==2.1.3                   # עיבוד אודיו
scipy==1.14.1                  # שמירת WAV

# --- ממשק ורקע (שלבים 4-5) ---
pywebview==5.3.2               # חלון דסקטופ (ממשק ווב)
pystray==0.19.5               # אייקון במגש המערכת
Pillow==11.0.0                 # יצירת אייקון המגש
